import * as A from "../actionTypes";

const initialState = {
  path: '/'
};

export default function(state = initialState, action) {
  switch (action.type) {
    default:
      debugger;
      return state;
  }
}
