{
  "name": "bidorbooserver",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": "^8.9.1",
    "npm": "^5.5.1"
  },
  "scripts": {
    "start": "npm install && npm run prod ",
    "start-server-prod": "npm run start --prefix server",
    "start-client-prod": "npm run start-js --prefix client",
    "prod": "npm install && cd server && yarn install && cd.. && cd client && yarn install && cd.. && concurrently \"npm run start-server-prod\" \"npm run start-client-prod\"",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix client && npm run build --prefix client"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^3.5.1",
    "yarn": "^1.3.2"
  }
}
